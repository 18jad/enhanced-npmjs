{
  "name": "npmjs-pnpm-button",
  "version": "1.0.0",
  "description": "npmjs-pnpm-button",
  "scripts": {
    "build": "cd src && tsc && cp styles ../build -r",
    "build:prod": "cd src && tsc && cp styles ../build -r && npm run zip",
    "zip": "7z a -tzip enhanced-npmjs.zip ."
  },
  "keywords": [],
  "author": "Jad Yahya",
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "^0.0.224"
  }
}
